name=TreadmillRemote
version=1.0.0
author=kolos
maintainer=kolos
sentence=Arduino library for controlling the Formill FT-21 treadmill via ESP8266 RF transmitter.
paragraph=Implements the fixed 54-bit header plus 27-bit command protocol (PLUS, MINUS, STOP, START, MODE) with precise pulse timing using delayMicroseconds(). Provides simple methods like start(), stop(), plus(), minus(), and mode() for easy integration into Arduino sketches.
category=Device Control
url=https://github.com/kolos/treadmill-remote-protocol
architectures=esp8266
